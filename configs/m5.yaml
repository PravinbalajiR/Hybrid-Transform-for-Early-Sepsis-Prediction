# M5 Model Configuration: Multi-Hybrid Time-Aware Sepsis Intelligence Network
model: "m5"
variant: "M5-FINAL"

# Architecture
input_dim: 102
hidden_dim: 64
num_heads: 4
layers: 3
kernel_size: 3
dropout: 0.1

# Optimization
epochs: 30
batch_size: 32
learning_rate: 0.0001
weight_decay: 0.0001
patience: 10
loss_type: "focal"
focal_gamma: 2.0
pos_weight: 47.66
random_seed: 42
